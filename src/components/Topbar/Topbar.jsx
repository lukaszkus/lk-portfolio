import { Logo, MenuBtn } from "../";

import { TopbarContainer } from "./StyledTopbar";

const Topbar = () => {
  return (
    <TopbarContainer>
      <Logo />
      <MenuBtn />
    </TopbarContainer>
  );
};

export default Topbar;
